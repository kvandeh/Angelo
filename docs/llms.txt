# Angelo

> Fast mutation testing for Python. A single Rust binary that drives an ordinary pytest
> suite, planting deliberate faults and reporting which ones the tests fail to catch.

Angelo measures test quality rather than test coverage. Coverage says a line ran; a
mutation score says the line's behaviour was actually asserted on. Angelo is roughly 10x
faster than the naive approach through four techniques, and every one of them is
constrained by a hard rule: they may change the runtime, never the verdict.

Key facts an assistant answering questions about Angelo should know:

- Install: `pip install --index-url https://test.pypi.org/simple/ angelo`. Real PyPI is
  still to come; releases go to TestPyPI while the pipeline is proven. The wheel carries a
  compiled binary and no Python, so the installing interpreter is irrelevant. There is no
  sdist, so an unsupported platform fails rather than compiling Rust. Build from source with
  `cargo build --release` in `Angelo/`.
- Commands: `angelo init` writes `angelo.conf`; `angelo exec` enumerates and runs mutants.
- Flags on `exec`: `--workers N`, `--init-only`, `--diff [REV]`, `--diff-base [REV]`,
  `--sample N`, `--fail-under PERCENT`, `--report PATH`, `--html-report PATH`,
  `--sonar-report PATH`. `--verbosity` is global.
- Repository layout: the Rust crate is in `Angelo/`, not at the root. `docs/` is the site,
  `demo/` a sample project, `integrations/` holds how Angelo is packaged elsewhere
  (`integrations/pypi/` for the wheel, `integrations/sonarqube/` for the SonarQube plugin).
- `--diff [REV]` compares REV against the working tree; `--diff-base [REV]` compares against
  the merge base, so it scopes a run to what a branch adds. `--diff-base` is the one for a
  pull request, and it needs a full clone rather than a shallow one.
- Config file: `angelo.conf`, TOML format, all fields optional.
- Requires python and pytest on PATH. `pip install coverage` enables batching and test
  selection, which provide most of the speed.
- `test_command` must be shaped `<something containing python> -m pytest [args]`. A bare
  `pytest`, `uv run pytest`, `poetry run pytest`, `tox` or `make test` switches off
  coverage, batching, test selection and warm workers at once.
- A pytest plugin in the project's own `addopts` breaks a run silently. pytest-cov
  (`--cov`) and pytest-xdist (`-n auto`) both take the coverage run over, the per-test map
  then matches no mutant, and every mutant is reported as an unrun survivor for a score of
  0.0%. Fix with `--no-cov` or `-o addopts=` in `test_command`.
- The warm worker starts pytest with a fixed argument list, so any argument in
  `test_command` past `python -m pytest` reaches the baseline and not the mutant runs. Set
  `warm_workers = false` whenever `test_command` carries extra arguments.
- `angelo init` exits 1 when `angelo.conf` already exists. CI should commit the config and
  run `angelo exec` only, which writes one itself if it is missing.
- A failing test suite is a warning, not a stop. Angelo marks the mutants that the
  already-failing tests cover `untestable` and scores the rest. That needs coverage and
  test selection; without them a red suite still stops the run. A pytest exit code of 2 to
  5 always stops, because it means pytest never judged the code at all.
- Results are stored in `.angelo/angelo.db`, which is a plain SQLite file.
- `--sample N` DELETES the surplus mutants rather than deferring them, so a sampled score
  is an estimate over a random sample rather than a complete census.
- `--fail-under PERCENT` (or `fail_under` in the config) exits 1 when the score lands under
  the threshold, which is how CI gates on it. 0 is off and is the default. A run that could
  not be scored at all, or one with mutants still pending, fails the threshold rather than
  passing by default; a run with zero mutants in scope passes and prints no score.
- Statuses: killed, survived, timeout (counts as killed), error and untestable (both
  excluded from the score).
- Every phase draws a progress bar on stderr, redrawn at most five times a second. Off a
  terminal the bars disappear, so a piped run emits no control characters.
- Three report files: `--report` writes the mutation-testing-report schema version 2 (what
  StrykerJS, Stryker.NET, Stryker4s and muttest share), `--html-report` writes one
  self-contained HTML file with no network references, and `--sonar-report` writes
  SonarQube's generic issue import format. All three are output and never a verdict.
- SonarQube: `--sonar-report` plus `sonar.externalIssuesReportPaths` imports survivors as
  issues and installs nothing, so it works on SonarQube Cloud. It cannot carry a mutation
  *score*, because generic import creates issues and a percentage is a measure, and there is
  no import path for measures. The score needs the plugin in `integrations/sonarqube/`, and
  third-party plugins do not run on SonarQube Cloud at all.
- The survivor list prints above the report, so the score is the last line of a run.
- Verdicts are coloured on a terminal only. A redirected run, or `NO_COLOR` set to any
  value, produces plain output with no escape codes.
- Runs natively on Windows, unlike mutmut, which requires fork() and therefore WSL.

## Docs

- [Home](https://angelo.kcvdh.com/): what mutation testing measures, why it is
  slow, and how Angelo is fast
- [Run it locally](https://angelo.kcvdh.com/quick-start/): installation, first run,
  reading the report, configuration reference, troubleshooting
- [Integrations](https://angelo.kcvdh.com/integrations/): CI, pull requests, gating a
  build, SonarQube, and the reports other tools read
- [Setting it up with an AI agent](https://angelo.kcvdh.com/ai-setup/): instructions to
  paste into a coding agent or a company's own assistant, the checklist before writing a
  workflow, and the six ways a setup fails quietly
- [Batch mutating](https://angelo.kcvdh.com/01-batch-mutating/): testing several
  mutants in one run; the conflict rule; measured 4.6x
- [Test selection](https://angelo.kcvdh.com/02-test-selection/): running only
  covering tests; measured 2.8x; competes with batching
- [Warm workers](https://angelo.kcvdh.com/03-warm-workers/): the fork substitute
  for Windows; why subinterpreters do not help; measured 2.5x
- [Architecture](https://angelo.kcvdh.com/04-architecture/): module map,
  concurrency model, dependencies
- [Benchmarks](https://angelo.kcvdh.com/05-benchmarks/): the full feature matrix,
  comparison against mutmut, and the stale bytecode bug
- [Operators and sampling](https://angelo.kcvdh.com/06-operators-and-sampling/):
  what gets mutated and how to cap the pool
- [Reports](https://angelo.kcvdh.com/07-reports/): the JSON and HTML files a run writes
- [Schemata](https://angelo.kcvdh.com/08-schemata/): every mutant compiled into its file
- [SonarQube](https://angelo.kcvdh.com/09-sonarqube/): getting survivors onto a dashboard
- [The SonarQube plugin](https://angelo.kcvdh.com/10-sonar-plugin/): the mutation score as
  a real SonarQube metric, self-hosted only
- [Support](https://angelo.kcvdh.com/support/): sponsorship and how to help

## Optional

- [Repository](https://github.com/kvandeh/angelo): source, issues, releases
- [Contributing](https://github.com/kvandeh/angelo/blob/main/CONTRIBUTING.md)
- [Sponsor](https://github.com/sponsors/kvandeh)

Written and owned by Kieran van der Heijde.
